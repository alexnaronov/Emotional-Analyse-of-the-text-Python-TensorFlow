# Emotional-Analyse-of-the-text-Python-TensorFlow
This repository implements a web application running on the Flask framework and TensorFlow. The application implements three models of neural networks: Perceptron, Convolutional neural network, Recurrent neural network for defining emotional analysis of text.
